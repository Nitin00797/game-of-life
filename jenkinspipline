pipeline {
    agent ('node') {
        label 'master'
    }
    stages ('first stage') {
        stage ('first step') {
            steps ('first step') {
                echo "$PATH"
                sh 'pwd'
                sh 'mvn clean package'

        }
     }
   }
}
