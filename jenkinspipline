pipeline {
    agent ('node') {
        label 'master'
    }
    tools {
        maven 'Maven 3.3.9'
    }
    stages ('first stage') {
        stage ('first step') {
            steps ('first step') {
                sh 'pwd'
                sh 'mvn clean package'

        }
     }
   }
}
