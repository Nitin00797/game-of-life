pipeline {
    agent ('node') {
        label 'master'
    }

    stages ('first stage') {
        stage ('first step') {
            steps ('first step') {
                mvn 'clean package'
        }
     }
   }
}
